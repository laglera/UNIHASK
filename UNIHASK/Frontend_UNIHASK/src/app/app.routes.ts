import { Routes } from '@angular/router';
import { MessageBubble } from './component/message-bubble/message-bubble';

export const routes: Routes = [
{
  path: "chat_test",
  component: MessageBubble,
}
];
